{
  "name": "eod-work-log",
  "version": "3.9.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node --check editor.mjs && node --check links.mjs && node --check model.mjs && node --check app.mjs && node --check pdf.mjs && node --check preview.mjs && node --test tests/*.test.mjs",
    "vendor": "node scripts/vendor.mjs"
  },
  "devDependencies": {
    "jspdf": "4.2.1",
    "jspdf-autotable": "5.0.8"
  }
}
